# models.yaml — canonical model artifact paths
# Used by agents and orchestrator for centralized reference.

models:
  currency:
    model_path: models/currency/mobilenet_v2.pt
    class_names_path: models/currency/class_names.json

  sms:
    model_path: models/sms/sms_model.pkl
    tfidf_path: models/sms/tfidf_vectorizer.pkl

  phishing:
    model_path: models/phishing/phishing_model.joblib

  transactions:
    model_path: models/transactions/paysim_model.joblib
