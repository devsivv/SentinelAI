# reports/

Generated investigation outputs (JSON/PDF/HTML), one folder per agent plus `fusion/` for
combined reports. `case_id` is the join key across every report here and every log entry
in `logs/` — always filter/search by it when debugging a specific case.
