# Sprint 00 — Project Planning `[MVP]`

Paste `PROJECT_CONTEXT.md` here first.

We are working on **Phase 0 — Project Planning (MVP)** of SentinelAI.

**Objective:** Design the entire system before writing any code.

**Scope for this sprint:**
- Finalize architecture, folder structure, DB schema, tech stack, REST API design (the Agent Contract), and agent definitions.
- Do NOT write agent inference code yet — this phase is planning only.

**Constraints:**
- Follow `SYSTEM_RULES.md` and `AI_GUIDELINES.md` exactly.
- Match the Agent Contract already defined in `docs/api.md` — don't redesign it.
- Install Neo4j only if Phase 7 (Graph Agent) is actually planned to be attempted; otherwise plan around the NetworkX fallback in `docs/database.md`.

**Deliverables:** Folder structure, architecture doc, README, roadmap, DB schema (`backend/db/schema.sql`), API docs.

**Done when:** Fully planned, agent contract and DB schema written, one demo scenario scripted on paper (sample SMS + sample transaction → expected fused verdict) — no implementation yet.

At the end, list what should change in `PROJECT_CONTEXT.md`, `CHANGELOG.md`, and `TODO.md`.
