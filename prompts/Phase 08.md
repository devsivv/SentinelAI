# Sprint 11 — Deployment `[Future scope — do not build]`

Paste `PROJECT_CONTEXT.md` here first.

**This phase is Future scope per `MASTER_PLAN.md`. Do not spend build time here beyond the local `docker-compose.yml` that already exists at the repo root.**

If asked to build Dockerfiles-per-agent, Nginx, Redis/Celery, CI/CD, or cloud deployment config during the hackathon window: politely decline the build and instead add/update the corresponding bullet list in `docs/deployment.md` and a slide note in `docs/presentation.md`. This is the most common way hackathon teams run out of time (`SYSTEM_RULES.md` §8) — the guardrail exists on purpose.

If this prompt is being used *after* the hackathon, for a genuine production push, treat `docs/deployment.md`'s "Production" section as the real spec and proceed normally under `SYSTEM_RULES.md` / `AI_GUIDELINES.md`.
