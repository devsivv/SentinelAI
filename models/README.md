# models/

Trained model artifacts actually in use by each agent, one folder per domain. Git-ignored
(see root `.gitignore`) — per `SYSTEM_RULES.md` §3, commit code and configs, not weights.
Which experiment a model was promoted from is tracked in `configs/models.yaml`.
